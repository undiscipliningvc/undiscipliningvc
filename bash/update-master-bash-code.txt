# copying latest changes from dev to your dev branch:

git fetch --all && git checkout dev && git pull && git push && git checkout dev-adrian && git merge dev --no-edit && git push

---

# push from your local branch to the live website

##3 for mac (going from your unique dev branch)

git fetch --all && git checkout dev-adrian && git pull && git push && git checkout dev && git merge dev-adrian --no-edit && git push && git checkout stage && git merge dev --no-edit && git push && git checkout master && git merge stage --no-edit && git push && git checkout dev-adrian

## for mac (going from dev):

git fetch --all && git checkout dev && git pull && git push && git checkout stage && git merge dev --no-edit && git push && git checkout master && git merge stage --no-edit && git push && git checkout dev

## for windows (going from your unique dev branch):

git fetch --all; git checkout dev-adrian; git pull; git push; git checkout dev; git merge dev-adrian --no-edit; git push; git checkout stage; git merge dev --no-edit; git push; git checkout master; git merge stage --no-edit; git push; git checkout dev-adrian

## for windows (going from dev):

git fetch --all; git checkout dev; git pull; git push; git checkout stage; git merge dev --no-edit; git push; git checkout master; git merge stage --no-edit; git push; git checkout dev